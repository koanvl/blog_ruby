<div class="container">
  <div class="p-4">
    <div class="flex justify-start flex-wrap gap-4">
      <% @posts.each do |post|%>
        <%= render partial: "post_preview", locals: { post: post } %>
      <% end %>
    </div>
  </div>
</div>

